import React from "react";

export const handleClicks = () => {
  const handleClick = () => {
    if (screen === 0) {
      setScreen(1);
    } else {
      setScreen(String(screen) + 1);
      Number(Screen);
    }
  };
};
